#include "main.h"
#include <stdio.h>

/**
* main - function that takes a pointer to an int
*
* Return: Always 0
*/
void reset_to_98(int *n)
{
	int *p;

	p = 98;
	print("Value of 'p' : %d\n", *p);

	return (0);
}
